# 日報ジェネレーター 詳細設計書

対象: `r-tsuruta-create/daily-report`
実装単位: `index.html` 単一HTML
最終更新: 2026-07-07

改訂履歴:

- 2026-07-07: 合計時間表示の描画責務をviewReport()からviewSubheader()へ移動。緑=通常サイズを明記。
- 2026-07-06: 並び替え（Pointer Events方式）、スクロール位置維持、稼働時間色分け、タコスのメンション形式変更、テンプレート/送信相手のインライン編集を追加。
- 2026-06-25: 初版確定。

## 0. 実装概要

- 実装版HTMLのUI構造、CSS、画面関数を土台にする。
- Firebase Auth、Firestore、Slack送信は旧 `index.html` の関数名を維持して移植する。
- すべてを1つの `index.html` に統合する。
- 外部ビルドや生成バンドルに依存しない。

主要関数:

```js
viewHeader()
viewSubheader()
viewReport()
viewTacos()
viewSettings()
viewNav()
render()
outputBlocks()
tacoPosts()
googleLogin()
googleLogout()
userDocRef()
loadFromFirestore()
saveToFirestore()
debouncedFirestoreSave()
postToSlack(text)
```

## 1. データ設計

### 1-1. state

```js
state = {
  tab: 'report' | 'tacos' | 'settings',
  day: 'today' | 'tomorrow',
  today: Category[],
  tomorrow: Category[],
  greeting: string,
  presets: string[],
  contacts: TacoContact[],
  templates: TacoTemplate[],
  blocks: TacoBlock[],
  tacoMode: 'merge' | 'split',
  accumulated: number,
  usageDate: string,
  webhook: string,
  theme: 'light' | 'dark',
  tone: string
}
```

### 1-2. 日報

```js
Category = {
  id: number,
  category: string,
  tasks: Task[]
}

Task = {
  id: number,
  hours: number,
  name: string
}
```

- `today` は退勤報告として出力する本日分。
- `tomorrow` は朝会報告として出力する明日分。
- `groupsOf()` は現在選択中のタブのデータを返す。
- `state.day === 'today'` では `today`。
- `state.day === 'tomorrow'` では `tomorrow`。
- 出力では案件名に `＜＞` を付ける。

### 1-3. タコス

```js
TacoContact = {
  id: number,
  name: string,
  freq: number
}

TacoBlock = {
  id: number,
  recipients: string[],
  count: number,
  body: string
}

TacoTemplate = {
  id: string,
  label: string,
  body: string
}
```

- 宛先初期値は空。
- 実名やSlackユーザーIDはコードに書かない。

## 2. レイアウト設計

### 2-1. 固定シェル

- `.app` は `height: 100vh; height: 100dvh;`。
- `.header` は `position: fixed` で上部固定。
- `.subheader` は日報/タコス用の固定補助ヘッダー。
- `.nav` は `position: fixed` で下部固定。
- `.scroll` は絶対配置のスクロール領域。
- `.scroll` は下部ナビ分の `padding-bottom` を持つ。
- タブごとに `.app[data-tab="..."] .scroll` の `top` を切り替える。
- `.tab` には再描画時の `animation` を付けない。
- 入力、タスク追加、タコス変更などの通常操作で画面全体をフェードさせない。
- タスク名、案件名、挨拶文、タコス本文の `oninput` では `render()` を呼ばない。
- 日本語IMEの変換候補を維持するため、入力中のDOM差し替えを避ける。

### 2-1-1. スクロール位置の記録と復元

- `render()` の先頭で `.scroll` の `scrollTop` を記録する。
- `innerHTML` 差し替えの直後、同じ `render()` 内で同期的に `scrollTop` を復元する。
- 復元を `setTimeout` / `requestAnimationFrame` 等で遅延させない。遅延させると先頭が一瞬描画されるチラつきが出る。
- フォーカス位置の記録・復元と同じ場所（`render()` 内）に実装する。
- `App.go()`（タブ切り替え）のときだけ復元せず、`scrollTop = 0` で先頭表示にする。タブごとに直前のスクロール位置を覚える機能は付けない。

### 2-2. 日報タブ

- `viewSubheader()` で退勤報告/朝会報告セグメントとワンタッチ流用ボタンを表示する。
- `viewReport()` は `groupsOf()` の結果を編集対象として表示する。
- 退勤報告タブでは `today` を表示・編集する。
- 朝会報告タブでは `tomorrow` を表示・編集する。
- 挨拶文カードは `state.day === 'today'` のときだけ表示する。
- 朝会報告タブでは挨拶文カードを出さない。

### 2-2-1. 並び替え（ドラッグ）

- HTML5 Drag and Drop API（`draggable` / `ondragstart` / `ondragenter`）は使わない。マウス専用でタッチに反応しないため。
- Pointer Events（`pointerdown` / `pointermove` / `pointerup` / `pointercancel`）で実装し、マウスとタッチの両方に対応する。
- ハンドル要素（⠿）に CSS `touch-action: none` を指定し、掴んだハンドルに `setPointerCapture` する。
- 並び替え対象は、案件カテゴリ（カテゴリ一覧内）と、同一カテゴリ内のタスク。タスクはカテゴリをまたがない。
- `pointerdown` 時に、リスト内の各要素の位置・高さ・要素間の間隔を取得して保持する。
- ドラッグ中、掴んだ要素は `transform: translateY()` でポインタに追従させ、浮き上がりの影と軽い拡大（scale 1.03 程度）を付ける。
- ドラッグ中、他の要素は「新しい並び順から積み上げ計算した位置」へ `transform` で移動し、CSS transition でリアルタイムに避ける。
- ドラッグ中は `render()` を呼ばない。DOMの再生成を行わない（掴んだ要素が消えて中断する既知の不具合の原因）。
- タスクをドラッグ中は、親カードの `overflow` を一時的に `visible` にし、カード外への描画見切れを防ぐ。
- `pointerup` で並びを確定する。確定時は transition を一時的に無効化してから `transform` を解除し、DOM順序と `state` 配列（`today` / `tomorrow` の `Category[]`、または `Category.tasks`）の順序を更新する。着地のジャンプを出さない。
- 確定後は保存対象にする（`persist()`）。

### 2-2-2. 稼働時間の合計表示と色分け

- 合計時間表示（計 X.Xh）はviewSubheader()が描画する。viewReport()は合計時間を出力しない。
- 合計計算は共通関数に切り出し、viewSubheader()から呼ぶ。
- 日報タブの .app[data-tab="report"] .scroll のtopは、固定サブヘッダーの高さぶん下げる。
- 判定は0.5刻みの丸め後の合計で行う: Math.round(sum * 2) が 16 超なら赤、16 未満なら黄、16 ちょうどなら緑。
- 赤・黄のときは文字サイズを通常より大きくする。緑のときは通常サイズのまま。
- 赤は var(--danger)、緑は var(--ok)、黄はテーマ変数の警告色（ライト/ダーク両テーマ定義）。
- 合計計算は既存仕様どおり、タスク名が空のタスクを除外した合計とする。

### 2-3. ワンタッチ流用ボタン

- `App.quickCopy()` が担当する。
- Clipboard API は使わない。
- 退勤報告タブでは `朝会報告の内容をコピー` と表示する。
- 退勤報告タブで押すと `state.today = cloneGroups(state.tomorrow)` を実行する。
- 朝会報告タブでは `退勤報告の内容をコピー` と表示する。
- 朝会報告タブで押すと `state.tomorrow = cloneGroups(state.today)` を実行する。
- `cloneGroups()` は案件カテゴリとタスクを新しい `id` で複製する。
- 挨拶文はコピーしない。
- 実行後は `render()` し、保存対象にする。

### 2-4. タコスタブ

- `viewSubheader()` でタコス固定サブヘッダーを返す。
- タコス固定サブヘッダーは `class="subheader meter"` にする。
- 固定サブヘッダー内に `タコスを贈る` タイトルを表示する。
- 固定サブヘッダー内に `本日のタコス消費` と `入力中の消費見込み / 5` のゲージを表示する。
- ゲージ値は `tacoDraftConsumption()` で計算する。
- `.app[data-tab="tacos"] .scroll` は固定サブヘッダーの高さぶん下げる。
- `viewTacos()` の本文先頭には別の「タコスを贈る」見出しブロックを置かない。
- 本文はすぐ宛先+文面ブロックから始める。
- 出力プレビュー内で `merge` / `split` を切り替える。
- コピー時に `accumulated` を加算する。
- 設定画面には `accumulated` の手動調整欄を置かない。

### 2-5. 設定タブ

- 表示、Slack連携、Googleログイン、タコス送信相手、日報プリセット、タコステンプレートを表示する。
- Googleログインカードは `currentUser` を表示する。
- ログインボタンは `googleLogin()`、ログアウトボタンは `googleLogout()` に接続する。
- 本日のタコス消費の手動調整欄は表示しない。

### 2-5-1. タコス送信相手のインライン編集

- 連絡先行にえんぴつアイコンを追加する。編集UIは日報の案件名編集（えんぴつ→インライン入力→チェックで確定）と同じ方式。
- `state.editingContactId` を追加する。編集開始 `App.startEditContact(id)`、入力 `App.setContactName(id, v)`、確定 `App.stopEditContact()`。
- 名前入力の `oninput` では `render()` を呼ばない（IME対応）。確定時に `render()` と保存を行う。
- 空文字で確定した場合は元の名前に戻す。既存の別の連絡先と同名になる確定は拒否してトーストを出す。
- 名前編集は連絡先一覧と宛先プルダウンに反映する。入力中ブロック（`state.blocks[].recipients`）に追加済みの宛先チップには反映しない（旧名の文字列のまま残す。パス独立の原則）。

### 2-5-2. タコステンプレートのインライン編集

- テンプレート行にえんぴつアイコンを追加する。編集UIは 2-5-1 と同じ方式。
- `state.editingTemplateId` を追加する。編集開始 `App.startEditTemplate(id)`、入力 `App.setTemplateLabel(id, v)` / `App.setTemplateBody(id, v)`、確定 `App.stopEditTemplate()`。
- ラベルと文面の両方を編集できる。入力の `oninput` では `render()` を呼ばない（IME対応）。確定時に `render()` と保存を行う。
- ラベルまたは文面が空で確定した場合は元の値に戻す。
- 既存の追加用ハンドラ名（`setTplLabel` / `setTplBody` = 新規追加欄）と編集用ハンドラ名を混在させない。別名にする。

## 3. 出力設計

### 3-1. formatDay(groups)

- 案件カテゴリごとに `＜案件＞` を出力する。
- タスクごとに `X.Xh｜タスク名` を出力する。
- タスク名が空のタスクは出力しない。

### 3-2. outputBlocks()

```js
{
  block1: '退勤報告',
  block2: '今日やったこと',
  block3: '明日やること'
}
```

- `block1` は `greeting + today`。
- `block2` は `today` のみ。
- `block3` は `tomorrow` のみ。
- 区切り線は含めない。

### 3-3. copyBlock / copyAll

- 出力プレビュー内の個別コピーは Clipboard API を使う。
- `copyBlock(1)` は退勤報告を文章コピーする。
- `copyBlock(2)` は今日やったことを文章コピーする。
- `copyBlock(3)` は明日やることを文章コピーする。
- `copyAll()` は3まとまりを文章コピーする。
- これは `quickCopy()` とは別機能。

## 4. タコス設計

### 4-1. tacoPosts()

- 入力済みの宛先、本文、個数から投稿配列を作る。
- `merge` では同じ個数のブロックを1投稿にまとめる。
- `split` では宛先1人ごとに投稿を分ける。
- 宛先に「＠」を付けない。`'＠' + r` の連結をやめ、名前だけを出力する。
- 連名の宛先は `join('\n')` で1行1人の縦並びにする。スペース区切りの横並びにしない。
- 投稿文の構成は上から「宛先（1行1人）」「本文」「タコス絵文字」。
- 出力プレビューのカード内（モード切替の説明文の近く）に、ⓘ＋短文のヒントを常時表示する。文言:「Slackに貼り付け後、名前の前に @ を打つと候補が出ます」。開閉式にしない。

### 4-2. tacoDraftConsumption()

- 入力中ブロックの消費見込みを返す。
- 計算式は `宛先人数 × 個数`。
- 本文が未入力でも、宛先と個数があればゲージには反映する。
- 宛先追加、宛先削除、個数増減、ブロック削除で即時再描画される。

### 4-3. copyPost / copyAllTacos

- `copyPost(num)` は対象投稿を文章コピーし、消費数を加算する。
- `copyAllTacos()` は全投稿を文章コピーし、全投稿分の消費数を加算する。
- コピー前に `syncDailyUsage()` を呼び、日付が変わっていれば消費数をリセットする。
- 消費数を加算した直後に `render()` を呼び、固定サブヘッダーのゲージへ即時反映する。
- `tacoDraftConsumption() > 5` のとき、ゲージ背景・文字・バーを警告色にする。
- 個別コピー後に一括コピーした場合の二重加算は許容する。

## 5. 永続化設計

### 5-1. localStorage

- 未ログイン時とオフライン時のバックアップとして使う。
- 主キーは `daily_report_state_v5`。
- `webhook_url` と `theme` は旧キー互換として維持する。

### 5-2. Firestore

- 保存先は `users/{uid}`。
- `loadFromFirestore()` はユーザー文書を一括取得する。
- `saveToFirestore()` は `{ merge: true }` で保存する。
- `saving` / `saveQueued` で多重保存を防ぐ。
- `debouncedFirestoreSave()` は1500msの遅延保存。

保存キー:

- `reportWork`
- `reportPresets`
- `tacoContacts`
- `tacoTemplates`
- `tacoBlocks`
- `tacoUsage`
- `tacoMode`
- `webhookUrl`
- `theme`
- `accentColor`
- `updatedAt`

## 6. 認証・送信設計

### 6-1. Firebase Auth

- Firebase compat CDNを使う。
- `firebase.initializeApp(firebaseConfig)` を使う。
- `auth = firebase.auth()`。
- `db = firebase.firestore()`。
- `auth.onAuthStateChanged` を初期化時に登録する。
- `setPersistence` は呼ばない。

### 6-2. Slack

- `postToSlack(text)` は `fetch`、`mode: 'no-cors'`、`payload=` 形式を使う。
- 日報のSlack送信は `block1`、`block2`、`block3` を順に送る。
- タコスはSlack送信しない。

## 7. 実装済みチェック

- [x] 実装版HTMLのCSS/HTML構造を土台にした。
- [x] 認証・同期・送信関数名を維持した。
- [x] Firestoreを新構造にした。
- [x] 旧データ移行分岐を入れていない。
- [x] 下部ナビを固定した。
- [x] ヘッダーを固定した。
- [x] 退勤報告タブは `today` を表示・編集する。
- [x] 朝会報告タブは `tomorrow` を表示・編集する。
- [x] 退勤報告タブだけ挨拶文カードを表示する。
- [x] 朝会報告タブには挨拶文カードを表示しない。
- [x] `quickCopy()` は文章コピーではなく流用コピーにした。
- [x] `quickCopy()` は反対側の案件カテゴリ/タスクを現在タブへ上書きコピーする。
- [x] `quickCopy()` は挨拶文をコピーしない。
- [x] タコス固定サブヘッダーを `class="subheader meter"` にした。
- [x] タコス画面の空白位置にタイトルとゲージを表示した。
- [x] タコスゲージを入力中の宛先人数×個数に即時追従させた。
- [x] 入力中のタコス消費見込みが5個を超えたら警告色にする。
- [x] 設定画面から本日のタコス消費欄を削除した。
- [x] 通常操作時の画面本体フェードを削除した。
- [x] 文字入力中の再描画を抑え、IME変換候補が消えないようにした。
- [x] `setPersistence` を使っていない。
- [x] 同僚の実名やSlackユーザーIDをコードに書いていない。
- [x] 生成バンドル由来の痕跡、外部ツール由来の展開タグ、不要なエスケープ済み文字列を含めていない。

2026-07-06 追加分（実装時にチェックする）:

- [ ] 並び替えをPointer Eventsで実装した。HTML5 DnD（`draggable` / `ondragstart` / `ondragenter`）を使っていない。
- [ ] ハンドルに `touch-action: none` と `setPointerCapture` を使った。
- [ ] 掴んだ要素がポインタに追従して浮き、他の要素がリアルタイムで避ける。
- [ ] ドラッグ中に `render()` を呼んでいない。
- [ ] タスクは同一カテゴリ内のみで移動し、カテゴリをまたがない。
- [ ] 並び替え確定で `state` 配列の順序を更新し、保存対象にした。
- [ ] `render()` でスクロール位置を同期的に記録・復元した。遅延実行にしていない。
- [ ] タブ切り替え時のみ先頭表示にした。
- [ ] 合計時間の色分け（8h超=赤・大、8h未満=黄・大、8h=緑・通常サイズ）を退勤・朝会の両方に実装した。
- [ ] タコス投稿の宛先から「＠」を外し、連名を1行1人の縦並びにした。
- [ ] 出力プレビュー近くにメンション方法のⓘヒントを常時表示した。
- [ ] タコス送信相手のインライン編集を実装した（空文字は元に戻す、重複名は拒否）。
- [ ] タコステンプレートのインライン編集を実装した（ラベル・文面とも、空は元に戻す）。
- [ ] 編集入力の `oninput` で `render()` を呼んでいない（IME対応）。

2026-07-07 追加分（挙動で確認する）:

- [ ] 日報タブでスクロールすると、合計時間は画面内の同じ位置に見え続ける。
- [ ] スクロール領域の中に合計時間の表示が存在しない。
- [ ] 合計を8.5hにすると赤・大、7.5hにすると黄・大、8.0hにすると緑・通常サイズになる。
