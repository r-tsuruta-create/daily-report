# MISTAKES.md — ミスパターン集（再発防止）

このファイルはAIエージェント（Codex等）が過去に犯したミスのパターン集である。
実装・修正作業の前に必ず全件読み、違反しないこと。違反状態でのリリースは禁止。

運用ルール:
- 1エントリ=「禁止ルール1行」「悪い例→良い例」「完了条件（挙動で検証可能なもの）」の3点で書く。
- 同類のミスは既存エントリに統合する。上限は20件。超える場合は古い・頻度の低いものから統合・削除する。
- 完了条件は必ず挙動型（「〜すると〜になる」）で書く。存在型（「〜がある」）で書かない。

---

## M-01: 位置・固定・レイアウト要件はDOM構造（親要素・描画関数）で指定・実装する

**禁止**: 「固定して」「ヘッダに表示」のような曖昧な位置指定のまま実装を確定させること。

- 悪い例: 「合計時間の表示をヘッダに固定して」→ 色とサイズだけ直して位置はスクロール領域内のまま、という部分実装が起きた。
- 良い例: 「合計時間は `.subheader`（固定ヘッダー、`viewSubheader()` が描画）内に配置。スクロール領域 `[data-tab="report"] .scroll` 内には置かない・重複させない」

**完了条件**: スクロール操作後も対象要素が画面内の同じ位置に見えることをE2Eで確認し緑であること。

## M-02: 検証は変数名・内部実装を見ない。ユーザーから見える挙動をアサートする

**禁止**: 変数名・関数名・特定文字列の存在チェックを「検証」として扱うこと。検証を通すために実装（変数名等）を検証側へ合わせて戻すこと。

- 悪い例: `dayHoursState` という文字列がコード内に存在するかをチェック（verify_v6.mjs事故）。位置が間違っていても緑になり、正しいリファクタリングで赤になった。
- 良い例: スクロール前後で要素の boundingBox が不変であることをPlaywrightでアサート。

**完了条件**: 変数名だけを変更するリファクタリングを行ってもテストが落ちないこと。仕様違反（位置・色・サイズの誤り）を入れるとテストが落ちること。
